// Not to be confused with the import x = require("name") syntax used to load
// modules, this syntax simply creates an alias for the specified symbol. You
// can use these sorts of imports (commonly referred to as aliases) for any kind
// of identifier, including objects created from module imports.
import S = Satchel;
